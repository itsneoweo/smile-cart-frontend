info:
  name: Get product details
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/products/:slug"
  params:
    - name: slug
      value: infinix-inbook-2
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
