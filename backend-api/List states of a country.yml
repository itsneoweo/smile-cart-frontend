info:
  name: List states of a country
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}/states?country_code=IN"
  params:
    - name: country_code
      value: IN
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
